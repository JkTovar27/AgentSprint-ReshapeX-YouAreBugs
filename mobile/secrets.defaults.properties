# Committed template for secrets.properties. Copy this file to secrets.properties
# and adjust per machine; secrets.properties is gitignored and always wins.
#
# 10.0.2.2 is how the Android emulator reaches localhost on the host machine.
# For a physical device use your host's LAN IP, e.g. http://192.168.1.50:8000
BACKEND_BASE_URL=http://10.0.2.2:8000
